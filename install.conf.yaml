- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.zsh_history:

#- create:
#    - ~/Work

- shell:
    -
#  - [git submodule update --init --recursive, Installing submodules]
